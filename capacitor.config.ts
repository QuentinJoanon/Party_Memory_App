import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'party_memory',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
